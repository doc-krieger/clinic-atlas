---
phase: 02-source-ingestion
plan: 04
type: execute
wave: 3
depends_on: ["02-02", "02-03"]
files_modified: []
autonomous: false
requirements:
  - SRCI-01
  - SRCI-02
  - SRCI-03
  - SRCI-05

must_haves:
  truths:
    - "PDF upload works end-to-end: file selected, progress shown, source appears in list"
    - "URL fetch works end-to-end: URL submitted, content extracted, source appears in list"
    - "Scanned PDF warning is visible in the UI and persisted in database quality_flags"
    - "SearXNG search returns results, user can select and ingest serially"
    - "Duplicate upload is rejected with reference to existing source"
    - "SSRF attempt is blocked with error message"
    - "All tests pass with zero xfails and zero skips"
  artifacts: []
  key_links:
    - from: "frontend/src/components/sources/pdf-upload-tab.tsx"
      to: "http://localhost:8000/api/sources/upload"
      via: "POST SSE stream"
      pattern: "/api/sources/upload"
    - from: "frontend/src/components/sources/source-list.tsx"
      to: "http://localhost:8000/api/sources"
      via: "GET JSON"
      pattern: "/api/sources"
---

<objective>
Integration verification: confirm the full source ingestion pipeline works end-to-end across frontend and backend, including negative cases. All four phase success criteria are checked. Zero xfail/skip test stubs remain.

Purpose: Catches integration issues (CORS, SSE streaming, file upload encoding, quality_flags persistence) that unit tests cannot detect. Verifies negative cases (SSRF, duplicate, scanned PDF) per review feedback.

Output: Verified working system ready for phase completion.
</objective>

<execution_context>
@/home/wihan/Projects/clinic-atlas/.claude/get-shit-done/workflows/execute-plan.md
@/home/wihan/Projects/clinic-atlas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-source-ingestion/02-CONTEXT.md
@.planning/phases/02-source-ingestion/02-REVIEWS.md
@.planning/phases/02-source-ingestion/02-02-SUMMARY.md
@.planning/phases/02-source-ingestion/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite, verify zero xfails/skips, and check API endpoints including negative cases</name>
  <files></files>
  <read_first>
    - backend/app/sources/router.py
    - backend/tests/test_ingestion.py
    - backend/tests/test_searxng.py
  </read_first>
  <action>
Addresses review concerns: Plan 04 must fail if scaffolded xfail tests remain, must include negative test cases.

1. **Verify zero xfails/skips remain** -- Run:
   ```bash
   docker compose exec backend uv run pytest tests/test_ingestion.py tests/test_searxng.py -v --tb=short 2>&1
   ```
   Scan output for `xfail`, `XFAIL`, `skip`, or `SKIPPED`. If ANY remain, this is a blocker -- the test was not implemented in Plan 02. Fix by implementing the missing test or escalate.

2. **Run full test suite** -- Run `make test` to verify all backend and frontend tests pass.

3. **Run lint** -- Run `make lint` to verify code quality.

4. **Verify API endpoints respond correctly** (positive cases):
   - `curl -s http://localhost:8000/api/sources | python3 -m json.tool | head -5` -- should return JSON with `"sources"` array and `"count"` key. Addresses review HIGH (GET source list).
   - `curl -s -X POST http://localhost:8000/api/sources/search -H "Content-Type: application/json" -d '{"query": "neonatal jaundice", "limit": 3}' | python3 -m json.tool | head -10` -- should return JSON with `"results"` array
   - `curl -s -X POST http://localhost:8000/api/sources/fetch -H "Content-Type: application/json" -d '{"url": "https://httpbin.org/html"}' 2>&1 | head -20` -- should return SSE events with `event: progress` and `event: complete`

5. **Verify negative cases** -- Addresses review MEDIUM (negative test cases in E2E):
   - **SSRF rejection**: `curl -s -X POST http://localhost:8000/api/sources/fetch -H "Content-Type: application/json" -d '{"url": "http://localhost:8000/api/health"}' 2>&1 | head -10` -- should return SSE error event about non-public IP or unsafe URL
   - **Duplicate upload**: Upload the same PDF twice via curl and verify second returns error event with `existing_source_id`
   - **Invalid file type**: `curl -s -X POST http://localhost:8000/api/sources/upload -F "file=@README.md;type=text/plain" 2>&1 | head -5` -- should return 400

6. **Verify Postgres migration applied cleanly**:
   - `docker compose exec postgres psql -U clinic_atlas -c "\d raw_sources" | grep -E "quality_flags|page_count|source_type|author"` -- all four columns must appear
   - `docker compose exec postgres psql -U clinic_atlas -c "\d raw_sources" | grep content_hash` -- must show UNIQUE constraint

7. **Fix any issues found** -- CORS headers, import errors, missing dependencies, type errors, migration issues.
  </action>
  <verify>
    <automated>cd /home/wihan/Projects/clinic-atlas && make test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `make test` exits 0
    - `make lint` exits 0 (or only warnings, no errors)
    - pytest output contains 0 lines with `xfail` or `XFAIL` or `skip` or `SKIPPED` (all stubs replaced with real tests)
    - GET /api/sources returns 200 with JSON body containing "sources" key and "count" key
    - POST /api/sources/search returns 200 with JSON body containing "results" key
    - POST /api/sources/fetch returns 200 with SSE event stream for valid URL
    - POST /api/sources/fetch with localhost URL returns SSE error event (SSRF blocked)
    - POST /api/sources/upload with non-PDF returns 400
    - raw_sources table has quality_flags, page_count, source_type, author columns
    - raw_sources table has UNIQUE constraint on content_hash
  </acceptance_criteria>
  <done>All tests pass with zero xfails/skips. API endpoints respond correctly for positive and negative cases. SSRF protection verified. Database schema has all required columns and constraints.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User verifies end-to-end source ingestion</name>
  <files></files>
  <read_first>
    - .planning/phases/02-source-ingestion/02-UI-SPEC.md
  </read_first>
  <action>Present the verification steps to the user and wait for approval. Fix any issues the user reports.</action>
  <what-built>Complete source ingestion pipeline: PDF upload with drag-and-drop, URL fetch with progress streaming, SearXNG search with result selection and serial batch ingestion, quality gate warnings (scanned PDF, thin content) persisted to database, source list fetched from API with status badges, SSRF protection on URL fetch.</what-built>
  <how-to-verify>
1. Open http://localhost:3000 in your browser
2. Click "Sources" in the sidebar -- should navigate to /sources with active highlight
3. **PDF Upload test (SRCI-01):**
   - Drag a text PDF file onto the drop zone (or click "Drop a PDF here" to browse)
   - Watch the progress bar fill with status updates ("Parsing...", "Indexing...", "Source indexed")
   - Source should appear in the list below with "PDF" badge and "Indexed" status
4. **Duplicate upload test (D-02):**
   - Upload the same PDF again
   - Should see inline error: "This document has already been ingested."
5. **URL Fetch test (SRCI-02):**
   - Switch to "Fetch URL" tab
   - Enter a URL like https://cps.ca/en/documents/position/neonatal-screening (or any accessible clinical page)
   - Click "Fetch" -- watch progress streaming ("Fetching...", "Extracting content...", "Source indexed")
   - Source should appear in list with "URL" badge
6. **Search test (SRCI-05):**
   - Switch to "Search Sources" tab
   - Search for a clinical topic like "neonatal jaundice"
   - Results should appear with domain badges and checkboxes
   - Select one or more results, click "Ingest N selected"
   - Watch per-item progress: spinner -> checkmark for each URL processed serially
   - "Ingesting 1 of 3..." counter updates as each completes
7. **Quality warnings (SRCI-03):**
   - If you have a scanned PDF, upload it -- should show yellow warning "scanned images"
   - Source list should show "Warning" badge (persisted, not just during ingestion)
   - Try a paywalled URL (e.g., uptodate.com article) -- should show thin content warning
8. **Source list persistence:**
   - Refresh the page (F5)
   - Previously ingested sources should still appear in the list (fetched from API, not just local state)
  </how-to-verify>
  <verify>User approves the end-to-end flow</verify>
  <acceptance_criteria>
    - User confirms PDF upload works with progress streaming
    - User confirms duplicate rejection shows error message
    - User confirms URL fetch works with progress streaming
    - User confirms SearXNG search returns results and serial batch ingest works
    - User confirms quality warnings appear for scanned/thin content
    - User confirms source list persists across page refresh
  </acceptance_criteria>
  <done>User has verified all four ingestion pathways work correctly in the browser, including negative cases (duplicate, quality warnings) and persistence across page refresh.</done>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Integration verification only -- no new code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| N/A | N/A | N/A | N/A | No new threats -- verification plan only. SSRF protection verified in Task 1 negative cases. |
</threat_model>

<verification>
- `make test` exits 0 with zero xfails and zero skips
- `make lint` exits 0
- All four phase success criteria from ROADMAP.md verified:
  1. User can upload a PDF and see it appear as an indexed raw source
  2. User can submit a URL and see it fetched, extracted, and indexed as a raw source
  3. Uploading a scanned/image-only PDF surfaces a visible flag (and quality_flags persisted in DB)
  4. SearXNG can be queried for a trusted source domain and returns results
- Negative cases verified: SSRF blocked, duplicate rejected, invalid file type rejected
- GET /api/sources returns indexed sources with quality_flags
</verification>

<success_criteria>
Phase 2 complete: all ingestion pathways work end-to-end, quality gates surface warnings and persist to database, source list displays ingested sources from API with correct status badges derived from quality_flags, SSRF protection active, all tests pass with zero stubs. User has verified the system works visually and functionally.
</success_criteria>

<output>
After completion, create `.planning/phases/02-source-ingestion/02-04-SUMMARY.md`
</output>
