---
phase: 02-source-ingestion
plan: 04
type: execute
wave: 3
depends_on: ["02-02", "02-03"]
files_modified: []
autonomous: false
requirements:
  - SRCI-01
  - SRCI-02
  - SRCI-03
  - SRCI-05

must_haves:
  truths:
    - "PDF upload works end-to-end: file selected, progress shown, source appears in list"
    - "URL fetch works end-to-end: URL submitted, content extracted, source appears in list"
    - "Scanned PDF warning is visible in the UI"
    - "SearXNG search returns results, user can select and ingest"
  artifacts: []
  key_links:
    - from: "frontend/src/components/sources/pdf-upload-tab.tsx"
      to: "http://localhost:8000/api/sources/upload"
      via: "POST SSE stream"
      pattern: "/api/sources/upload"
---

<objective>
Integration verification: confirm the full source ingestion pipeline works end-to-end across frontend and backend. All four phase success criteria are checked.

Purpose: Catches integration issues (CORS, SSE streaming, file upload encoding) that unit tests cannot detect.

Output: Verified working system ready for phase completion.
</objective>

<execution_context>
@/home/wihan/Projects/clinic-atlas/.claude/get-shit-done/workflows/execute-plan.md
@/home/wihan/Projects/clinic-atlas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-source-ingestion/02-CONTEXT.md
@.planning/phases/02-source-ingestion/02-02-SUMMARY.md
@.planning/phases/02-source-ingestion/02-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and verify API endpoints</name>
  <files></files>
  <read_first>
    - backend/app/sources/router.py
    - backend/tests/test_ingestion.py
    - backend/tests/test_searxng.py
  </read_first>
  <action>
1. Run `make test` to verify all backend and frontend tests pass.
2. Run `make lint` to verify code quality.
3. Verify the three API endpoints respond:
   - `curl -X POST http://localhost:8000/api/sources/search -H "Content-Type: application/json" -d '{"query": "test", "limit": 3}'` -- should return JSON with results array (may be empty if SearXNG has no results)
   - `curl -X POST http://localhost:8000/api/sources/fetch -H "Content-Type: application/json" -d '{"url": "https://httpbin.org/html"}' 2>&1 | head -20` -- should return SSE events
4. Fix any issues found -- CORS headers, import errors, missing dependencies, type errors.
  </action>
  <verify>
    <automated>cd /home/wihan/Projects/clinic-atlas && make test 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `make test` exits 0
    - `make lint` exits 0 (or only warnings, no errors)
    - POST /api/sources/search returns 200 with JSON body containing "results" key
    - POST /api/sources/fetch returns 200 with SSE event stream
  </acceptance_criteria>
  <done>All tests pass, linting clean, API endpoints respond correctly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User verifies end-to-end source ingestion</name>
  <files></files>
  <action>Present the verification steps to the user and wait for approval. Fix any issues the user reports.</action>
  <what-built>Complete source ingestion pipeline: PDF upload with drag-and-drop, URL fetch with progress streaming, SearXNG search with result selection, quality gate warnings (scanned PDF, thin content), and source list display.</what-built>
  <how-to-verify>
1. Open http://localhost:3000 in your browser
2. Click "Sources" in the sidebar -- should navigate to /sources
3. **PDF Upload test:**
   - Drag a PDF file onto the drop zone (or click to browse)
   - Watch the progress bar fill with status updates
   - Source should appear in the list below with "PDF" badge and "Indexed" status
4. **URL Fetch test:**
   - Switch to "Fetch URL" tab
   - Enter a URL like https://cps.ca/en/documents/position/neonatal-screening (or any accessible clinical page)
   - Click "Fetch" -- watch progress streaming
   - Source should appear in list with "URL" badge
5. **Search test:**
   - Switch to "Search Sources" tab
   - Search for a clinical topic like "neonatal jaundice"
   - Results should appear with domain badges
   - Select one or more results, click "Ingest selected"
   - Watch per-item progress indicators
6. **Quality warnings:**
   - If you have a scanned PDF, upload it -- should show yellow warning
   - Try a paywalled URL (e.g., uptodate.com) -- should show thin content warning
  </how-to-verify>
  <verify>User approves the end-to-end flow</verify>
  <done>User has verified all four ingestion pathways work correctly in the browser.</done>
  <resume-signal>Type "approved" or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Integration verification only -- no new code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| N/A | N/A | N/A | N/A | No new threats -- verification plan only |
</threat_model>

<verification>
- `make test` exits 0
- All four phase success criteria from ROADMAP.md verified:
  1. User can upload a PDF and see it appear as an indexed raw source
  2. User can submit a URL and see it fetched, extracted, and indexed as a raw source
  3. Uploading a scanned/image-only PDF surfaces a visible flag
  4. SearXNG can be queried for a trusted source domain and returns results
</verification>

<success_criteria>
Phase 2 complete: all ingestion pathways work end-to-end, quality gates surface warnings, source list displays ingested sources with correct status badges. User has verified the system works visually and functionally.
</success_criteria>

<output>
After completion, create `.planning/phases/02-source-ingestion/02-04-SUMMARY.md`
</output>
