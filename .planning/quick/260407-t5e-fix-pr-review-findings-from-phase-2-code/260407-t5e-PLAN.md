---
type: quick
description: Fix PR review findings from phase 2 code review
tasks: 3
---

<objective>
Fix all verified PR review findings across planning docs, backend, and frontend from the phase 2 code review. Each finding must be verified against current code before applying a fix — skip any that are already resolved or do not reproduce.

Purpose: Close out PR review feedback so phase 2 can ship clean.
Output: All fixable findings resolved, tests passing.
</objective>

<execution_context>
@/home/wihan/Projects/clinic-atlas/.claude/get-shit-done/workflows/execute-plan.md
@/home/wihan/Projects/clinic-atlas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-source-ingestion/02-REVIEW.md
@.planning/phases/02-source-ingestion/02-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix planning doc lint and consistency issues</name>
  <files>
    .planning/phases/02-source-ingestion/.continue-here.md
    .planning/phases/02-source-ingestion/02-03-PLAN.md
    .planning/phases/02-source-ingestion/02-03-SUMMARY.md
    .planning/phases/02-source-ingestion/02-04-PLAN.md
    .planning/phases/02-source-ingestion/02-CONTEXT.md
    .planning/phases/02-source-ingestion/02-DISCUSSION-LOG.md
    .planning/phases/02-source-ingestion/02-REVIEW.md
    .planning/phases/02-source-ingestion/02-UAT.md
    .planning/ROADMAP.md
    .planning/STATE.md
  </files>
  <action>
Read each file and verify the finding exists before fixing. Apply fixes only where the issue reproduces:

1. `.continue-here.md` ~line 47-49: Add `bash` language tag to any fenced code block missing one (MD040).
2. `02-03-PLAN.md` ~line 528-532: Fix STRIDE table rows T-02-06/T-02-07 — ensure pipe characters and column alignment are correct (MD055/MD056).
3. `02-03-SUMMARY.md` line 116: Change "full stack integration" to "full-stack integration" (hyphenate compound modifier).
4. `02-04-PLAN.md` ~line 177-180: Fix threat model table row with missing pipe delimiters.
5. `02-CONTEXT.md` ~line 14-17: Fix heading-level jump — headings must not skip levels (e.g., h2 to h4). Insert an h3 or change h4 to h3.
6. `02-DISCUSSION-LOG.md` ~line 15-139: Add blank lines before and after markdown table blocks (MD058).
7. `ROADMAP.md` line 119: Update Source Ingestion progress from "0/4" to match actual completed plans (should be "4/4" since all 4 plans have SUMMARYs).
8. `STATE.md`: Fix inconsistency between frontmatter (completed_phases: 2, completed_plans: 7, percent: 100) and body (Plan: Not started, Status: Ready to execute, Progress: 0%). Update body to reflect that Phase 2 is complete: Plan = "All complete", Status = "Phase 2 complete", Progress bar filled, velocity table includes phase 2. Also update "Current focus" to reflect post-phase-2 status.
9. `02-REVIEW.md` ~line 94-105: If WR-01 finding exists and was fixed (check 02-REVIEW-FIX.md), mark it as resolved.
10. `02-UAT.md` ~line 63-74: If gap status shows "failed", update to "fixed" (the gap was fixed per commit history).
  </action>
  <verify>
    <automated>cd /home/wihan/Projects/clinic-atlas && grep -n "Progress:.*0%" .planning/STATE.md; echo "EXIT:$?" ; grep -n "0/4" .planning/ROADMAP.md; echo "EXIT:$?"</automated>
  </verify>
  <done>All planning doc findings verified and fixed where applicable. No "0/4" in ROADMAP for phase 2, no inconsistent state in STATE.md, no MD040/MD055/MD056/MD058 violations in the listed files.</done>
</task>

<task type="auto">
  <name>Task 2: Fix backend bugs, security, and correctness issues</name>
  <files>
    backend/app/health/router.py
    backend/app/sources/router.py
    backend/app/sources/searxng.py
    backend/app/sources/service.py
    backend/app/config.py
    backend/Dockerfile
    backend/tests/test_ingestion.py
    backend/tests/test_schema.py
  </files>
  <action>
Read each file and verify the finding exists before fixing. Apply only where the issue reproduces:

11. `health/router.py` ~line 81-83: If `required_ok` iterates something other than `REQUIRED_SERVICES` directly (e.g., iterates a dict or wrong variable), fix to iterate `REQUIRED_SERVICES`.
12. `sources/router.py` ~line 97-163: Check SSE endpoint session lifecycle. If the DB session closes before `parse_pdf` runs (e.g., session used outside its context manager scope), restructure so the session remains open for the full parse operation. This is a real bug — ensure the session context wraps all DB operations.
13. `sources/router.py` ~line 129-132: Add `from e` to any bare `raise ... from None` or `raise SomeException(...)` that should chain the original exception for traceback clarity.
14. `Dockerfile` ~line 22-35: Check if Playwright browser install path is accessible to the non-root runtime user. If browsers are installed as root but the app runs as a different user, fix by ensuring the `PLAYWRIGHT_BROWSERS_PATH` env var points to a shared location or install as the runtime user.
15. `tests/test_ingestion.py` ~line 223-243: Check `test_file_size_limit` — if `app.dependency_overrides` is modified but not restored in a finally block, wrap in try/finally to restore the original overrides dict.
16. `tests/test_schema.py` ~line 36-59: Read the current expected column set and compare against the actual schema (check alembic migrations or SQLModel models). Add any missing columns to the expected set.
17. `config.py` ~line 29-34: Add `ge=1` or `gt=0` Field validators to numeric config values that should never be zero or negative (e.g., `max_file_size_mb`, `chunk_size`, timeouts).
18. `searxng.py` line 48: Replace `lstrip("www.")` with `removeprefix("www.")`. `lstrip` strips individual characters, not a prefix — it would incorrectly strip leading w's or dots from domains like "wwww.example.com" or "wow.example.com".
19. `service.py` ~line 376-382: In `_route_handler`, add a guard for `None` hostname before using it (e.g., `if not hostname: raise ValueError(...)`).
20. `service.py` ~line 172-177: Check `parse_pdf` — if it receives a `Path` but expects `str` (or vice versa), fix the type to match the caller. Check both the function signature and all call sites.
21. `service.py` ~line 106-114: Check `handle_async_request` — if it does a synchronous DNS lookup (e.g., `socket.getaddrinfo`), wrap in `asyncio.to_thread()` or use `aiodns`/`loop.getaddrinfo()` to avoid blocking the event loop.
22. `service.py` ~line 402-410: Fix `quality_flags.remove("thin_content")` — if the flag might not be in the list, use `discard("thin_content")` instead of `remove()` to avoid `ValueError`.
  </action>
  <verify>
    <automated>cd /home/wihan/Projects/clinic-atlas && make test-backend 2>&1 | tail -30</automated>
  </verify>
  <done>All backend findings verified and fixed where applicable. Backend tests pass. No lstrip("www.") usage, no unguarded .remove() calls, session lifecycle correct in SSE endpoint, config fields have lower-bound validation.</done>
</task>

<task type="auto">
  <name>Task 3: Fix frontend bugs, React Query migration, and accessibility</name>
  <files>
    CLAUDE.md
    frontend/src/components/layout/sidebar.tsx
    frontend/src/components/sources/pdf-upload-tab.tsx
    frontend/src/components/sources/source-list-item.tsx
    frontend/src/components/sources/source-list.tsx
    frontend/src/components/sources/source-tabs.tsx
    frontend/src/components/sources/url-fetch-tab.tsx
    frontend/src/components/sources/search-tab.tsx
    frontend/src/components/ui/dialog.tsx
    frontend/src/components/ui/tabs.tsx
    frontend/src/lib/sse.ts
  </files>
  <action>
Read each file and verify the finding exists before fixing. Apply only where the issue reproduces:

23. `CLAUDE.md` ~line 166-176: Check if Context7 CLI references use `ctx7@latest` — if so, update to `ctx7` (the @latest is no longer needed per current usage). Apply throughout the file.
24. `sidebar.tsx` ~line 14-15: Add `aria-current="page"` attribute to the currently active navigation link for screen reader accessibility.
25. `pdf-upload-tab.tsx` ~line 180-190: Check if `onSourceAdded()` is called twice (e.g., both after SSE complete and in a success handler). Remove the duplicate call — keep only one invocation point.
26. `pdf-upload-tab.tsx` line 21: Check if `setCompleteData` state setter exists but is never read/used. If unused, remove the state variable entirely.
27. `source-list-item.tsx` ~line 21-33: Fix `formatRelativeTime`: (a) handle NaN from invalid dates (add a guard), (b) fix pluralization — ensure "1 day ago" not "1 days ago" etc.
28. `source-list.tsx` ~line 16-42: If using manual `fetch` + `useEffect` for loading sources, replace with `@tanstack/react-query` `useQuery` hook. Use a query key like `["sources"]` and return data from the fetch function. This enables cache invalidation from other components.
29. `source-tabs.tsx` ~line 12-17: If using a `refreshKey` state/counter pattern to trigger re-fetches, replace with React Query's `queryClient.invalidateQueries({ queryKey: ["sources"] })`. Remove the refreshKey state.
30. `url-fetch-tab.tsx` line 33: Check abort controller cleanup — ensure the AbortController is properly cleaned up in the useEffect return function or on component unmount. If the abort is missing from cleanup, add it.
31. `dialog.tsx` ~line 66-70: Add `type="button"` to dialog close button(s) to prevent accidental form submission when dialog is inside a form.
32. `tabs.tsx` ~line 8-22: Check if the `orientation` prop is forwarded to `TabsPrimitive.Root`. If not, add it to the destructured props and pass it through.
33. `search-tab.tsx` ~line 48-59: Add error state handling to the search handler — wrap the fetch/search call in try/catch, set an error state, and display it to the user.
34. `sse.ts` ~line 93-101: Add a `console.warn` in development mode (`process.env.NODE_ENV === 'development'`) when receiving malformed SSE frames (e.g., JSON parse failures) to aid debugging.

For findings 28-29 (React Query migration): These are coupled changes. source-list.tsx must use useQuery, and source-tabs.tsx must use invalidateQueries instead of refreshKey. Ensure react-query is already installed (`@tanstack/react-query` in package.json) and a QueryClientProvider wraps the app. If not already set up, this migration may need to be deferred — note it and skip.
  </action>
  <verify>
    <automated>cd /home/wihan/Projects/clinic-atlas && make test-frontend 2>&1 | tail -30</automated>
  </verify>
  <done>All frontend findings verified and fixed where applicable. Frontend builds and tests pass. No duplicate onSourceAdded calls, no unused state, formatRelativeTime handles NaN and pluralization, dialog close buttons have type="button", accessibility attributes added.</done>
</task>

</tasks>

<verification>
After all tasks:
1. `make test` — all backend and frontend tests pass
2. `make lint` — no new lint errors introduced
3. Git diff review — changes are minimal and targeted (no unrelated modifications)
</verification>

<success_criteria>
- All 34 findings verified against current code
- Findings that reproduce are fixed
- Findings already resolved are skipped (no unnecessary changes)
- All tests pass
- No regressions introduced
</success_criteria>

<output>
After completion, create `.planning/quick/260407-t5e-fix-pr-review-findings-from-phase-2-code/260407-t5e-SUMMARY.md`
</output>
